package com.android.approjects.permissions.camera;

import android.content.Context;

class Camera2Api extends Camera2 {
    public Camera2Api(Callback callback, PreviewImpl preview, Context context) {
        super(callback, preview, context);
    }
}
